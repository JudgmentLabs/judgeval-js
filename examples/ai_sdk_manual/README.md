# ai_sdk_manual

AI SDK example using manual `Tracer` instrumentation (`Tracer.observe`,
`Tracer.span`, `Tracer.setInput`, `Tracer.setOutput`) instead of the AI SDK's
`experimental_telemetry` integration.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
